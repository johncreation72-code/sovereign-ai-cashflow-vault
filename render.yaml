services:
  - type: web
    name: sovereign-enterprise-core
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node global_master_supervisor.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
